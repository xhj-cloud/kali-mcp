# 2026-08-28 清理死代码（4 行）与 Kali 部署目录遗留文件

> 本文件是 kali-mcp 的一次更新说明，记录本次**新增了什么、修复了什么、改动了什么**。
> 命名与存放约定见 [docs/updates/README.md](README.md)。

### 背景

2026-08-13 死代码审计与后续重构遗留了两类"僵尸"文件/代码，一直未清：

1. **Kali 部署目录遗留旧文件**：`/home/xhj/桌面/kali-mcp/pentest.py`（73876 字节，2026-08-13 12:36）。它是重构前旧版 `pentest.py` 的拷贝，不在 `src/kali_mcp/` 包内，`python -m kali_mcp.server` 不会加载，但对人工排查有误导性（容易误以为是生效代码）。
2. **4 行死代码**（上次审计只记了 3 行，重新全仓库 grep 核实实际是 4 行）：
   - `src/kali_mcp/ipv6.py:330` `_PING_RTT_RE` —— 定义后全仓库零引用；
   - `src/kali_mcp/ipv6.py:332` `_PING_RCVD_RE` —— 定义后全仓库零引用；
   - `src/kali_mcp/pentest.py` `ddos_attack` 内 `import time as _time` —— `_time` 全文零使用；
   - `src/kali_mcp/pentest.py` `packet_sniff` 内 `import time as _time` —— 同上（审计时漏数的一行）。
   - 同组 `_PING_LOSS_RE`/`_PING_AVG_RE` 确认在用（`ipv6_ping` 解析输出），未动。

### 📝 改动

- 删除 `ipv6.py` 中 `_PING_RTT_RE`、`_PING_RCVD_RE` 两行正则定义。
- 删除 `pentest.py` 中 2 处函数内 `import time as _time`。
- 删除 Kali 部署目录遗留文件 `/home/xhj/桌面/kali-mcp/pentest.py`（`sudo rm`，服务不读该文件，零风险）。
- 删除 Kali 部署目录 3 个 macOS scp 产生的 AppleDouble 残留：`src/._kali_mcp`、`src/kali_mcp/._server.py`、`src/kali_mcp/._pentest.py`（非代码文件，Python 不会加载；今后部署用 `COPYFILE_OFF=1 scp ...` 可避免再生）。
- **测试：234 passed**（数量不变，纯删除无行为变化）。
- 工具总数不变（66），所有工具签名与输出结构完全兼容。

### ✅ 部署与验证

- 本地 `pytest` 234 全过后提交、双远程推送（origin Gitea + github）。
- Kali 部署：scp 更新后的 `ipv6.py`/`pentest.py` → `sudo cp` 实时目录 → md5 两边一致 → `systemctl restart kali-mcp` → active。
- 线上验证：`tools/list` 仍为 **66 个工具**；`ddos_attack`（非法 method 探针）、`packet_sniff`（短时长）实调正常。
- 遗留文件删除后确认：`/home/xhj/桌面/kali-mcp/pentest.py` 不存在，`src/kali_mcp/pentest.py` 完好。

---
